manifestVersion: 1
name: "@lint/sonarr"
version: "2026.05.21.2"
description: "Sonarr v3 wrapper — snapshot series/root folders/tags (with statistics flattened for ended-but-incomplete queries), and delete series via the official API (files + import-list exclusion)."
repository: "https://github.com/sock-lint/swamp-extensions"
paths:
  base: manifest
models:
  - sonarr.ts
additionalFiles:
  - README.md
  - LICENSE.txt
labels:
  - sonarr
  - tv
  - arr
  - homelab
platforms: []
