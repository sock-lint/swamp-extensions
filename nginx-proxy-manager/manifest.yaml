manifestVersion: 1
name: "@lint/nginx-proxy-manager"
version: "2026.05.21.2"
description: "Nginx Proxy Manager API wrapper — snapshot proxy hosts / redirection hosts / certificates, upsert proxy hosts idempotently (match by domain set), and delete proxy hosts by id."
repository: "https://github.com/sock-lint/swamp-extensions"
paths:
  base: manifest
models:
  - nginx-proxy-manager.ts
additionalFiles:
  - README.md
  - LICENSE.txt
labels:
  - nginx
  - reverse-proxy
  - networking
  - npm
platforms: []
