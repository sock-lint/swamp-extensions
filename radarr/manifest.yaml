manifestVersion: 1
name: "@lint/radarr"
version: "2026.05.21.2"
description: "Radarr v3 wrapper — snapshot movies/root folders/tags (with on-disk IMDb parsing for drift detection), and delete movies via the official API (files + import-list exclusion)."
repository: "https://github.com/sock-lint/swamp-extensions"
paths:
  base: manifest
models:
  - radarr.ts
additionalFiles:
  - README.md
  - LICENSE.txt
labels:
  - radarr
  - media
  - arr
  - homelab
platforms: []
