manifestVersion: 1
name: "@lint/adguard"
version: "2026.05.21.1"
description: "AdGuard Home control-API wrapper — snapshot status/stats/clients/rewrites and reconcile DNS rewrites to a desired set."
paths:
  base: manifest
models:
  - adguard.ts
additionalFiles:
  - README.md
  - LICENSE.txt
labels:
  - dns
  - networking
  - adguard
  - homelab
platforms: []
